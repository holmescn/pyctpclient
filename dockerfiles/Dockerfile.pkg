FROM ubuntu:18.04

ENV LD_LIBRARY_PATH /usr/local/lib
RUN sed -i "s/archive.ubuntu.com/mirrors.163.com/" /etc/apt/sources.list \
    && apt-get update \
    && apt-get upgrade python3 \
    && apt-get install -y build-essential libboost-all-dev python4-dev python3-venv python3-pip

WORKDIR /tmp

